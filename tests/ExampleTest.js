const chai = require('chai');
const sinon = require('sinon');
const assert = require('assert');

describe('We can do stuff', () => {
    it('should do things', (done) => {
        assert.equal(true, true);
        done()
    })
});